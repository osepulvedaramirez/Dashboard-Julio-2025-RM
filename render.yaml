services:
  - type: web
    name: dashboard-julio-2025-rm # Puedes cambiar este nombre si quieres
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:server
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9